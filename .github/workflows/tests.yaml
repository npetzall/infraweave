name: Tests

on:
  pull_request:
    branches:
      - "*"
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Free up disk space
        uses: ./.github/actions/free-disk-space

      - name: Set up Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true

      - name: Rust Cache
        uses: Swatinem/rust-cache@f13886b937689c021905a6b90929199931d60db1 # v2.8.1
        with:
          shared-key: "build"
          save-if: ${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}

      - name: Build with warnings as errors
        run: cargo build --all-targets
        env:
          RUSTFLAGS: "-D warnings"

  lint:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          components: rustfmt, clippy
          override: true

      - name: Run cargo fmt
        run: cargo fmt --check

      - name: Run cargo clippy
        run: cargo clippy --no-deps --all-targets

  unit-tests:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Free up disk space
        uses: ./.github/actions/free-disk-space
        with:
          level: "maximum"

      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@9d84900f3238fab8cd84ce47d658d25dd008be2f
        with:
          tofu_version: 1.11.4

      - name: Set up Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true

      - name: Rust Cache
        uses: Swatinem/rust-cache@f13886b937689c021905a6b90929199931d60db1 # v2.8.1
        with:
          shared-key: "unit-tests"
          save-if: ${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}

      - name: Run unit tests
        run: cargo test --workspace --exclude integration-tests

  integration-tests:
    if: false
    runs-on: ubuntu-latest
    strategy:
      matrix:
        provider: [aws, azure]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Free up disk space
        uses: ./.github/actions/free-disk-space

      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@v1
        with:
          tofu_version: 1.9.1

      - name: Alias tofu as terraform
        run: sudo ln -sf $(which tofu) /usr/local/bin/terraform

      - name: Set up Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true

      - name: Rust Cache
        uses: Swatinem/rust-cache@f13886b937689c021905a6b90929199931d60db1 # v2.8.1
        with:
          shared-key: "integration-${{ matrix.provider }}"
          save-if: ${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}

      - name: Run integration tests for ${{ matrix.provider }}
        run: |
          make ${{ matrix.provider }}-integration-tests

  docker-builds:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - package: cli
            dockerfile: cli/Dockerfile
          - package: gitops
            dockerfile: gitops/Dockerfile.lambda
          - package: reconciler
            dockerfile: reconciler/Dockerfile.lambda
          - package: terraform_runner
            dockerfile: terraform_runner/Dockerfile.terraform
          - package: terraform_runner
            dockerfile: terraform_runner/Dockerfile.tofu
          - package: webserver-openapi
            dockerfile: webserver-openapi/Dockerfile
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build ${{ matrix.package }} package using ${{ matrix.dockerfile }}
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: false
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}/build-cache:${{ matrix.package }}-${{ hashFiles(matrix.dockerfile) }}
          cache-to: >-
            ${{ 
              github.event_name == 'push' 
              && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) 
              && format('type=registry,mode=max,ref=ghcr.io/{0}/build-cache:{1}-{2}', github.repository, matrix.package, hashFiles(matrix.dockerfile)) 
              || '' 
            }}

  python-builds:
    if: false
    name: Build python ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - name: linux-x86_64
            os: ubuntu-latest
            arch: x86_64
            python-arch: x64
            cibw-build: cp311-manylinux_x86_64
          - name: macos-arm64
            os: macos-latest
            arch: arm64
            python-arch: arm64
            cibw-build: cp311-macosx_arm64
          # - name: linux-aarch64
          #   os: ubuntu-latest
          #   arch: aarch64
          #   python-arch: arm64
          #   cibw-build: cp311-manylinux_aarch64
          # - os: macos-latest
          #   arch: x86_64
          #   python-arch: x64
          # - os: ubuntu-latest
          #   arch: arm64
          #   python-arch: arm64
          # - os: macos-latest
          #   arch: x86_64
          #   python-arch: x64
          #   - os: windows-latest
          #     arch: x86_64  # Only x86_64 is supported for Windows
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12
          architecture: ${{ matrix.python-arch }}
          cache: "pip"

      - name: Install Rust
        uses: dtolnay/rust-toolchain@fcf085fcb4b4b8f63f96906cd713eb52181b5ea4

      - name: Restore cibuildwheel cache
        uses: actions/cache/restore@v4
        with:
          path: ~/.cache/cibuildwheel
          key: ${{ runner.os }}-${{ matrix.arch }}-cibw-${{ hashFiles('infraweave_py/Cargo.toml', '**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.arch }}-cibw-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install maturin==1.10.2 cibuildwheel

      - name: Check Maturin Version
        run: maturin --version

      - name: Build Wheels
        env:
          CIBW_ARCHS: ${{ matrix.arch }}
          CIBW_BUILD: ${{ matrix.cibw-build }}
          CIBW_ENVIRONMENT: |
            PATH=$HOME/.cargo/bin:$PATH
            RUSTFLAGS=""
          CIBW_BEFORE_BUILD_LINUX: |
            yum install -y perl-core
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
            source $HOME/.cargo/env
          CIBW_BEFORE_BUILD_MACOS: |
            brew install openssl
        run: |
          cibuildwheel --output-dir dist infraweave_py

      - name: Upload Wheels
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}-wheels
          path: dist/*.whl
      
      - name: Save cibuildwheel cache
        if: ${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}
        uses: actions/cache/save@v4
        with:
          path: ~/.cache/cibuildwheel
          key: ${{ runner.os }}-${{ matrix.arch }}-cibw-${{ hashFiles('infraweave_py/Cargo.toml', '**/Cargo.lock') }}
